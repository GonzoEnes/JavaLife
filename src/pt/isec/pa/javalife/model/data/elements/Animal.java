package pt.isec.pa.javalife.model.data.elements;

import pt.isec.pa.javalife.model.data.area.Area;

public final class Animal extends Fauna {

    public Animal(Area area, Elemento tipo, int x, int y) {
        super(area, tipo, x, y);
    }
}
